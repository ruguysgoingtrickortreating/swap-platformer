extends Node2D


# Called when the node enters the scene tree for the first time.
#func _ready():
	##$StaticBody2D/CollisionPolygon2D/Polygon2D.polygon = $StaticBody2D/CollisionPolygon2D.polygon
	#for area:Area2D in $areas.get_children():
		#print(area)
		#area.get_node("StaticBody2D/Polygon2D").polygon = area.get_node("StaticBody2D/CollisionPolygon2D").polygon
		#area.body_entered.connect(func area_entered(body):
			#$Camera2D.position = area.get_node("ColorRect").position
			#
			#)
